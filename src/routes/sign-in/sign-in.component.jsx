import { signInWithGooglePopup } from '../../utils/firebase/firebase.utils';

const SignIn = () => {
    const logUser  = async () =>{
        const reponse = await 
    }
    return (
    <div>
      <h1>Sign in</h1>
    </div>
  );
};
export default SignIn;
